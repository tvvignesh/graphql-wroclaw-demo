{
  "name": "test-network-1",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ test-network-1",
    "create-local": "graph create --node http://localhost:8020/ test-network-1",
    "remove-local": "graph remove --node http://localhost:8020/ test-network-1",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 test-network-1"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.25.3",
    "@graphprotocol/graph-ts": "0.24.1"
  }
}
